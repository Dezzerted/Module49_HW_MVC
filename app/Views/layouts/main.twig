<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
    <link rel="stylesheet" href="/assets/css/styles.css">
</head>
<body>
    <nav>
        <a href="/" class="{{ current_path == '/' ? 'active' }}">Домашняя страница</a>
        <a href="/about" class="{{ current_path == '/about' ? 'active' }}">О нас</a>
        <a href="/contacts" class="{{ current_path == '/contacts' ? 'active' }}">Контакты</a>
        <a href="/php-gd-info" class="{{ current_path == '/php-gd-info' ? 'active' }}">Пакет php-gd</a>
    </nav>
    
    <div class="container">
        {% block content %}{% endblock %}
    </div>
</body>
</html>